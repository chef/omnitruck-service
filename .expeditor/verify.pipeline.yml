steps:
  - label: ":go: build unit tests"
    command:
      - yum install golang
      - go version
      - make test
    expeditor:
      executor:
        docker:
  - label: ":go: build syntax"
    command: 
      - yum install golang
      - go version
      - make check
    expeditor:
      executor:
        docker:

