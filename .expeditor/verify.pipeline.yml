env:

steps:
  - label: ":go: build unit tests"
    command:
      - make test
    expeditor:
      executor:
        docker:
  - label: ":go: build syntax"
    command: make check
    expeditor:
      executor:
        docker:

